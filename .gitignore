# ── Rust ──────────────────────────────────────────────────
**/target/
**/*.rs.bk
# Cargo.lock 入库（v4 起可复现构建；库 crate 不受影响）
# 例外：冻结的 v3 backend/ 目录维持独立构建，lock 不入库
backend/Cargo.lock

# ── Node ──────────────────────────────────────────────────
node_modules/
frontend/dist/
pnpm-lock.yaml

# ── Environment ────────────────────────────────────────────
.env
.env.local
.env.*
frontend/.env*
e2e/.env*

# ── IDE / Editor ──────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── OS ────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Build / Dependency artifacts ──────────────────────────
*.wav
*.mp3
*.bak
*.orig

# ── Startup scripts (platform-specific) ───────────────────
*.vbs
*.ps1
start_*.bat
start-*.bat

# ── AI / Agent development tools ──────────────────────────
.lingma/
.agents/
.um.agents/
.opencode
.codegraph
.sisyphus
# Agent-generated docs stay local except the tracked ADR + v4 plans (the
# README links them; they must ship with the repo). `docs/*` (not `docs/`)
# keeps the directory itself traversable for the negations below.
docs/*
!docs/adr/
!docs/compose/
docs/adr/*
!docs/adr/decisions.md
docs/compose/*
!docs/compose/plans/
docs/compose/plans/*
!docs/compose/plans/2026-08-28-mimotts-workbench-rebuild.md
!docs/compose/plans/2026-08-28-mimo-adaptation-research.md
.playwright-mcp/

# ── Logs ──────────────────────────────────────────────────
*.log
npm-debug.log*
*_stderr.txt
*_stdout.txt
*_err.txt
*_out.txt
backend_run*.txt
server_*.txt
backend_release_*.txt
backend_v*.txt

# ── Test artifacts ────────────────────────────────────────
test/
test-results/
test-screenshots/
playwright-report/
playwright/.cache/
e2e/.auth/
e2e-report/
*-test.ps1
*TEST-REPORT.md
*.test.*
# v4 frontend unit tests must be tracked (v3 legacy `*.test.*` rule is scoped out)
!apps/web/src/**/*.test.ts
!apps/web/src/**/*.test.tsx
sse_test.txt
test_cancel.rs
test-browser.js
test-browser.mjs
frontend/public/snapshot-*

# ── Runtime data ──────────────────────────────────────────
data/
.localref/

# ── v4 rebuild hygiene (legacy artifacts committed pre-v4) ──
backend/task_texts.db
backend/text_files/
.omo/
stress_results/
frontend/dist/
apps/web/dist/
packages/contract/generated/
e2e-v4/.state.json
e2e-v4/test-results/
